development:
  adapter: postgresql
  database: openbound_development
  mysql2pool: 5
  encoding: utf8
  reconnect: false
  username: ob_dev
  password: ob
  host:  localhost
  # socket: /tmp/mysql.sock

test:
  adapter: postgresql
  database: openbound_test
  pool: 5
  encoding: utf8
  username: ob_dev
  password: ob
  host:  localhost

production:
  adapter: postgresql
  encoding: utf8
  database: <%= ENV["DB_NAME"] %>
  username: <%= ENV["DB_USERNAME"] %>
  password: <%= ENV["DB_PASSWORD"] %>
  # host: <%= ENV["DB_HOST"] %>
  # port: <%= ENV["DB_PORT"] %>
  # socket: #TODO


# test:
#   adapter: mysql2
#   database: openbound_test
#   pool: 5
#   encoding: utf8
#   username: root
#   password: 
#   host:  localhost

# # This information is duplicated in /config/rubber/common/database.yml, 
# # which pulls environment rubber_env variables from /config/rubber/rubber.yml. 
# # These should be set to the host machine instead of hardcoded, but there they are, 
# # sucka. 

# # production:
# #   adapter: mysql2
# #   # encoding: utf8
# #   # database: <%= ENV["RDS_DB_NAME"] %>
# #   # username: <%= ENV["RDS_USERNAME"] %>
# #   # password: <%= ENV["RDS_PASSWORD"] %>
# #   # host: <%= ENV["RDS_HOSTNAME"] %>
# #   # port: <%= ENV["RDS_PORT"] %>

# production:
#   adapter: mysql2
#   encoding: utf8
#   database: <%= ENV["DB_NAME"] %>
#   username: <%= ENV["DB_USERNAME"] %>
#   password: <%= ENV["DB_PASSWORD"] %>
#   host: localhost # <%= ENV["RDS_HOSTNAME"] %>
#   port: 3306 #/var/run/mysqld/mysqld.sock
#   socket: /var/run/mysqld/mysqld.sock
#   # port: 3306 #<%= ENV["RDS_PORT"] %>


# # This should be different than production. Junk data. 
# staging:
#   adapter: mysql2
#   # encoding: utf8
